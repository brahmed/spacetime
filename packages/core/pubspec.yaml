name: core
description: Shared models, enums, exceptions, and utilities for SpaceTime.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  equatable: ^2.0.5
  json_annotation: ^4.11.0

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.8.0
  lints: ^5.0.0
  test: ^1.24.0
