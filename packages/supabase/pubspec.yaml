name: supabase_client
description: Supabase client, config, and repositories for SpaceTime.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  core:
    path: ../core
  supabase_flutter: ^2.7.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
